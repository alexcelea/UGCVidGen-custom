#!/bin/bash

# Shortcut script to launch the story generator
# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Change to the script directory first, then run the Python script
cd "$SCRIPT_DIR"
python3 "$SCRIPT_DIR/scripts/story_generator.py" "$@" 